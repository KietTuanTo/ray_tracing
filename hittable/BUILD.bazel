load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "hittable",
    hdrs = [
        "hittable.h",
    ],
    deps = [
        "//interval",
        "//vec3",
        "//ray",
    ],
    visibility = [
        "//visibility:public",
    ],
)